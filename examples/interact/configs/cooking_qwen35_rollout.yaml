interact:
  max_decisions: 200
  worker_timeout: 1800
  inference_timeout: 240
  fixed_eval_sampling_seed: 20260914
  multimodal_request_format: text
  # Keep processed frame tensors out of Ray/learner host memory. They are
  # materialized lazily one microbatch at a time during policy updates.
  spill_visual_inputs: true
